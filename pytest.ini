[pytest]
# Collect only the real test suite. tests/fixtures/ holds synthetic scan-target
# repositories (some containing their own test_*.py files, e.g. the readiness-*
# fixtures' test_adversarial.py) that are analysis inputs, not tests for this
# package — collecting them causes duplicate-basename import errors.
testpaths = tests
norecursedirs = fixtures
